<!DOCTYPE html>
<html>
     <body>
        <a href="/">Home</a>
        <% if (result) { %>
          <h2><%= result.model %> Details</h2>
          <li>Make: <%= result.make %></li>
          <li>Year: <%= result.year %></li>
      <% } else { %>
        <h2><%= (result) %> not found</h2>
      <% } %>
      
    </body>
</html>
